# Agents

- ChatGPT plans, reviews, and coordinates the work.
- Codex implements scoped tasks.
- Keep the project simple.
- Avoid unnecessary files and imports.
- Use `pd.read_csv` directly in notebooks.
- Create `.py` files only for reusable logic.
- Notebooks should contain analysis, visuals, and explanations.
- The final report/app should not contain code.
- After each significant phase, update `PROJECT_STATE.md`.
