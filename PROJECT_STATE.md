# Project State

- Project type: customer segmentation project.
- Current state: final segmentation model locked and regenerated. The final model uses the compact business feature set, `RobustScaler`, and K-Means with `k=5`. Basket profiling has not been rebuilt yet after the final model refresh.
- Completed work: initial EDA; feature engineering; preprocessing; advanced EDA and feature-selection review; K-Means baseline; K-Means vs GMM comparison; sampled Agglomerative and DBSCAN diagnostics; decision notebooks simplified and renamed to `06b_kmeans_robustness_decision.ipynb`, `06c_feature_set_decision.ipynb`, and `06d_final_preprocessing_decision.ipynb`; final compact RobustScaler K-Means output regeneration; cleanup of obsolete nested output folders, generated figures, Python cache files, and premature association-rule artifacts.
- Validated outputs: `data/processed/selected_model_features.csv` has 33,038 rows, 21 columns, no duplicated `customer_id` values, no missing values, and exactly `customer_id` plus 20 compact model features; `outputs/customer_clusters.csv` has 33,038 rows, exactly `customer_id` and `cluster` columns, no duplicated `customer_id` values, no missing cluster values, and five clusters; `outputs/kmeans_metrics.csv`, `outputs/kmeans_cluster_profile_summary.csv`, `outputs/kmeans_cluster_spending_shares.csv`, `outputs/model_comparison_metrics.csv`, `outputs/model_comparison_cluster_balance.csv`, `outputs/segmentation_robustness_metrics.csv`, `outputs/segmentation_seed_stability.csv`, `outputs/segmentation_robustness_recommendation.csv`, `outputs/feature_set_sensitivity_metrics.csv`, and `outputs/feature_set_sensitivity_recommendation.csv` match the current final decision chain.
- Final model metrics: K-Means k=5 has silhouette score 0.2208, Calinski-Harabasz score 6347.16, Davies-Bouldin score 1.5392, and cluster sizes of 2,619, 18,385, 5,538, 3,552, and 2,944 customers.
- Known decisions/risks: basket data remains excluded from clustering and should only be used after cluster assignment; one final cluster contains about 55.65% of customers, so interpretation should acknowledge that the largest segment is broad; DBSCAN is not suitable as the main segmentation model under the tested grid and should only be mentioned as an outlier diagnostic.
- Recommended next task: create basket profiling by final cluster using `data/raw/customer_basket.csv` and the refreshed `outputs/customer_clusters.csv`, then save `outputs/cluster_basket_profile.csv` and `outputs/cluster_top_products.csv`.
- Blockers: none currently known.
