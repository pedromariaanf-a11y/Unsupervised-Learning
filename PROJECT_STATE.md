# Project State

- Project type: customer segmentation project.
- Current state: ready for basket-based profiling and targeted promotion analysis.
- Completed work today: reviewed repository consistency; confirmed initial EDA, feature engineering, preprocessing, advanced EDA, and feature selection are complete; created `DECISIONS.md`; cleaned stale notebook wording/output in `notebooks/02_feature_engineering.ipynb`; completed the K-Means baseline in `notebooks/05_kmeans_baseline.ipynb` with `SELECTED_K = 5`; evaluated `k=2` through `k=10`; saved clustering metrics and customer assignments; generated elbow, silhouette, cluster balance, Calinski-Harabasz, Davies-Bouldin, and PCA figures; added initial cluster profiling; added reusable clustering helper functions in `src/clustering.py`; generated cluster profile tables and profiling figures.
- Validated outputs: `data/processed/customer_features.csv`, `data/processed/customer_features_info.csv`, `data/processed/customer_features_model.csv`, and `data/processed/selected_model_features.csv` exist; customer-level outputs keep 33,038 customers; processed outputs have no duplicated `customer_id`; `customer_tenure` has no negative values; `selected_model_features.csv` has no missing values, excludes customer_basket-derived features, and was not modified during clustering; `outputs/clustering/kmeans_customer_clusters.csv` has 33,038 rows, no duplicated `customer_id`, and no missing `cluster` values; the profiling merge preserves all 33,038 clustered customers; K-Means metrics, assignment, profile summary, spending-share profile, evaluation figures, PCA figure, and profiling figures were generated.
- Known decisions/risks: `customer_info` is the base customer table; every customer must be preserved; `loyalty_card_number` is represented as `has_loyalty_card`; `customer_name` is excluded except for `degree_level`; the dataset maximum `year_first_transaction` is used as the reference year for `age` and `customer_tenure`; `k=5` is a baseline solution, not a guaranteed final solution; clustering separation metrics are moderate, so final model quality should be judged using both metrics and business interpretability; basket-derived features are useful for profiling and promotions but excluded from the first clustering baseline because 4,911 customers have no basket records; basket analysis should be treated as complementary evidence for profiling and promotions; location features were excluded from the first selected baseline to avoid dominating distance-based clustering.
- Recommended next task: add basket-based profiling and targeted promotion analysis using the existing K-Means clusters.
- Blockers: none currently known.
