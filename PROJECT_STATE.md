# Project State

- Project type: customer segmentation project.
- Current state: model comparison completed; K-Means k=5 is the strongest practical candidate so far; final cluster output exists; repository outputs have been simplified to the agreed flat CSV structure.
- Completed work: K-Means baseline with k=5; initial cluster profiling; PCA and t-SNE visualizations inside notebooks; K-Means vs GMM comparison; Agglomerative comparison on a reproducible sample; DBSCAN outlier diagnostic on the same sample; reusable clustering helper functions reviewed; advanced EDA visuals and feature-selection rationale improved without changing the selected feature set; obsolete intermediate processed files and nested output folders removed.
- Validated outputs: `outputs/customer_clusters.csv` has 33,038 rows, no duplicated `customer_id` values, no missing `cluster` values, and exactly `customer_id` and `cluster` columns; `data/processed/selected_model_features.csv` has 33,038 rows, 26 columns, no duplicated `customer_id` values, no missing values, and no basket-derived modelling features; flat K-Means and model-comparison CSV outputs exist in `outputs/`.
- Known decisions/risks: K-Means k=5 remains the strongest practical candidate so far; GMM did not improve the segmentation and produced one very large cluster; Agglomerative Clustering gave some support to a 5-cluster structure but did not outperform K-Means; DBSCAN is not suitable as the main segmentation model under the tested grid and should only be mentioned as an outlier diagnostic; final model choice still needs to be connected to business usefulness and promotion design; top-level `outputs/` is the only output folder.
- Recommended next task: use `outputs/customer_clusters.csv` for basket-based profiling, association rules, targeted promotion design, and final business interpretation.
- Blockers: none currently known.
