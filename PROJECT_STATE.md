# Project State

- Project type: customer segmentation project.
- Current state: model comparison completed; K-Means k=5 remains supported after robustness and feature-set sensitivity checks, but the current feature set is not fully confirmed because a simpler variant without categorical dummies performs modestly better; final cluster output exists and was not modified; repository outputs use the agreed flat CSV structure; basket profiling by cluster is completed.
- Completed work: K-Means baseline with k=5; initial cluster profiling; PCA and t-SNE visualizations inside notebooks; K-Means vs GMM comparison; Agglomerative comparison on a reproducible sample; DBSCAN outlier diagnostic on the same sample; reusable clustering helper functions reviewed; advanced EDA visuals and feature-selection rationale improved without changing the selected feature set; obsolete intermediate processed files and nested output folders removed; basket-level profiling by final cluster completed without refitting the clustering model; segmentation robustness checks completed for k=3 through k=8, seed stability, and simple feature sensitivity; feature-set sensitivity checks completed for interpretable group removals and compact feature variants.
- Validated outputs: `outputs/customer_clusters.csv` has 33,038 rows, no duplicated `customer_id` values, no missing `cluster` values, and exactly `customer_id` and `cluster` columns; `data/processed/selected_model_features.csv` has 33,038 rows, 26 columns, no duplicated `customer_id` values, no missing values, and no basket-derived modelling features; flat K-Means and model-comparison CSV outputs exist in `outputs/`; `outputs/cluster_basket_profile.csv` and `outputs/cluster_top_products.csv` exist and were generated from post-clustering basket analysis; `outputs/segmentation_robustness_metrics.csv`, `outputs/segmentation_seed_stability.csv`, `outputs/segmentation_robustness_recommendation.csv`, `outputs/feature_set_sensitivity_metrics.csv`, and `outputs/feature_set_sensitivity_recommendation.csv` exist.
- Known decisions/risks: K-Means k=5 remains supported by robustness and feature-set checks; k=6 is the closest alternative but does not clearly outperform k=5; the current 25-feature set is under review because `compact_business_set` / removing categorical dummies improves k=5 silhouette modestly while keeping similar cluster balance; `outputs/customer_clusters.csv` still reflects the current feature set and was not overwritten; DBSCAN is not suitable as the main segmentation model under the tested grid and should only be mentioned as an outlier diagnostic; basket profiling is based only on observed basket records, and 4,911 clustered customers have no basket records.
- Recommended next task: decide whether to keep the current feature set for final reporting or run a separate approved phase to rebuild the final K-Means k=5 output using the compact feature set before association rules.
- Blockers: none currently known.
