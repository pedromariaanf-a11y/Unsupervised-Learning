# Project State

- Project type: customer segmentation project.
- Current state: model comparison completed; K-Means k=5 remains supported as the practical final candidate after robustness checks; final cluster output exists and was not modified; repository outputs use the agreed flat CSV structure; basket profiling by cluster is completed.
- Completed work: K-Means baseline with k=5; initial cluster profiling; PCA and t-SNE visualizations inside notebooks; K-Means vs GMM comparison; Agglomerative comparison on a reproducible sample; DBSCAN outlier diagnostic on the same sample; reusable clustering helper functions reviewed; advanced EDA visuals and feature-selection rationale improved without changing the selected feature set; obsolete intermediate processed files and nested output folders removed; basket-level profiling by final cluster completed without refitting the clustering model; segmentation robustness checks completed for k=3 through k=8, seed stability, and simple feature sensitivity.
- Validated outputs: `outputs/customer_clusters.csv` has 33,038 rows, no duplicated `customer_id` values, no missing `cluster` values, and exactly `customer_id` and `cluster` columns; `data/processed/selected_model_features.csv` has 33,038 rows, 26 columns, no duplicated `customer_id` values, no missing values, and no basket-derived modelling features; flat K-Means and model-comparison CSV outputs exist in `outputs/`; `outputs/cluster_basket_profile.csv` and `outputs/cluster_top_products.csv` exist and were generated from post-clustering basket analysis; `outputs/segmentation_robustness_metrics.csv`, `outputs/segmentation_seed_stability.csv`, and `outputs/segmentation_robustness_recommendation.csv` exist.
- Known decisions/risks: K-Means k=5 remains supported by the robustness check, with the best baseline silhouette among k=3 through k=8 and very high seed stability; k=6 is the closest alternative and should be mentioned in the final discussion, but it does not clearly outperform k=5; DBSCAN is not suitable as the main segmentation model under the tested grid and should only be mentioned as an outlier diagnostic; basket profiling is based only on observed basket records, and 4,911 clustered customers have no basket records.
- Recommended next task: create association rules by cluster using the final clusters and basket data.
- Blockers: none currently known.
